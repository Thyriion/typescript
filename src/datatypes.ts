const nullValue = null; //kein Wert - meiner
const undefinedValue = undefined; //kein Wert - gehört der Engine

const x: void; // Kein Rückgabewert;
const y: never; //Kein Rückgabewert -- wirft immer fehler 